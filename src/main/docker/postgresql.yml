version: '2'
services:
  azimut-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/azimut/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=azimut
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
